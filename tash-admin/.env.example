# TASH admin — environment
# Base URL of the live FastAPI backend.
VITE_API_URL=http://localhost:8000
# Escape hatch: "true" -> use the in-memory mock (src/lib/api.mock.ts);
# anything else / unset -> real backend.
VITE_USE_MOCK=false
