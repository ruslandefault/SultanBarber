# Render Blueprint — TASH backend (FastAPI).
# Deploy: Render Dashboard → New → Blueprint → shu rep'ni tanlang.
# Render bu faylni o'qib, web service'ni avtomatik yaratadi.
# Baza: Neon (DATABASE_URL'ni deploy paytida so'raydi — sync:false).
services:
  - type: web
    name: tash-backend
    runtime: python
    plan: free
    rootDir: tash-backend
    region: frankfurt
    buildCommand: pip install -r requirements.txt
    # Migratsiya + (idempotent) seed + server. Seed mavjud salon bo'lsa o'tkazib yuboradi.
    startCommand: alembic upgrade head && python -m app.db.seed && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: DATABASE_URL # Neon connection string (postgresql://...) — deploy paytida joylang
        sync: false
      - key: BOT_TOKEN # @BotFather token
        sync: false
      - key: MINIAPP_URL # Telegram Mini App URL (client frontend manzili)
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: APP_TZ
        value: Asia/Tashkent
      - key: PYTHON_VERSION
        value: "3.12.7"

  # ── Client (Telegram Mini App) — statik SPA ──
  - type: web
    name: tash-client
    runtime: static
    rootDir: tash-client
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://sultanbarber.onrender.com
      - key: VITE_USE_MOCK
        value: "false"

  # ── Admin panel — statik SPA ──
  - type: web
    name: tash-admin
    runtime: static
    rootDir: tash-admin
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: https://sultanbarber.onrender.com
      - key: VITE_USE_MOCK
        value: "false"
